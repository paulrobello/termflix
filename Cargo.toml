[package]
name = "termflix"
version = "0.1.0"
edition = "2024"
description = "A library of terminal ASCII animations with colors, braille, half-block rendering, and procedural generation"

[dependencies]
crossterm = "0.29.0"
noise = "0.9.0"
clap = { version = "4.5.60", features = ["derive"] }
rand = "0.10.0"
